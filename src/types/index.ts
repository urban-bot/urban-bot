export type UrbanChat = {
    id: string;
};

export type UrbanFrom = {
    id?: string | number;
    username?: string;
    firstName?: string;
    surname?: string;
};

export type UrbanParseMode = 'HTML' | 'markdown';
